version: '3.8'

services:
  ryftaxi:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
    restart: unless-stopped
    # Uncomment to persist logs or mount configuration
    # volumes:
    #   - ./logs:/app/logs
